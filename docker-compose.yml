services:
  app:
    build: .
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    environment:
      - APP_ENV=development
    # Ensure dependencies are installed and migrations run (optional, adjust as needed)
    # command: sh -c "joss migrate && joss server start" 
